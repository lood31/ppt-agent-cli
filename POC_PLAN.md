# ppt-agent-cli 候选引擎 PoC 执行计划

状态：待执行  
日期：2026-08-12  
目标：用机器证据选择 `OfficeCLI`、`hands-on-deck` 或自研最小写入层

## 1. PoC 结论必须回答的问题

1. 哪个候选引擎能最可靠地读取和修改 WPS 生成的 PPTX？
2. 普通编辑后，已有动画、母版、图表和不支持对象是否仍能保留？
3. WPS COM 能否稳定添加、读取和修改最小动画集合？
4. WPS 能否在无交互条件下保存副本、重新打开和导出 PDF？
5. 哪个方案能以最少适配代码提供紧凑 inspect、原子 patch 和结构 diff？
6. 默认工作流是否有现实机会达到 70% Token 降幅？

PoC 不实现完整产品。任何与上述问题无关的功能都延后。

## 2. 已验证环境事实

- 操作系统：Windows。
- WPS Office：12.1.0.26895。
- 演示组件 COM ProgID：`KWPP.Application` 已注册。
- `PowerPoint.Application` 也存在，但可能是 WPS 兼容入口；PoC 优先使用明确的 `KWPP.Application`。
- 最终验收环境是 WPS，不要求 Microsoft PowerPoint。

所有 PoC 结果必须记录实际 WPS 版本和候选引擎 commit/release。

## 3. 候选方案

### A. iOfficeAI/OfficeCLI

待验证优势：

- OpenXML 直接操作；
- 机器可读 help/schema；
- 动画和多种 PPTX 对象能力；
- 单文件分发；
- Apache-2.0。

主要风险：

- 代码面与功能面较大；
- README 声明不等于 WPS 实测兼容；
- 内置渲染与 WPS 最终效果可能不同；
- 自动更新、联网和安装行为需要安全审计；
- 默认输出是否足够紧凑未知。

### B. EveryInc/hands-on-deck

待验证优势：

- 原子 JSON patch；
- 紧凑 inspect、结构 diff 和局部渲染；
- 原生 shape ID 寻址；
- 格式继承、lint 和确定性修复；
- Python 技术栈便于适配；
- MIT。

主要风险：

- 依赖 `python-pptx` 与 LibreOffice/Poppler 渲染链路；
- 当前明确不创建 shape animation；
- 修改复杂 WPS 文件后的保真度未知；
- 需要增加 revision、candidate、幂等和 WPS 最终验证。

### C. 自研最小 OOXML 写入层

只有 A、B 都未达到硬门槛时才评估。PoC 阶段不得提前投入完整自研。

## 4. 安全审计门槛

在运行任何候选引擎前：

1. 固定具体 commit 或 release，并记录来源 URL。
2. 核对许可证、NOTICE 和第三方依赖。
3. 审查安装脚本、自动更新、遥测、联网、子进程和默认写入目录。
4. 优先从源码运行或验证发布包 SHA-256。
5. 禁止运行时自动更新。
6. 禁止处理真实客户机密文件。
7. 所有测试只在专用临时目录与副本上执行。

任何存在未解释联网、隐蔽上传或不可关闭自动更新行为的候选方案，先暂停评估。

## 5. 测试材料

### 5.1 合成测试稿

创建一份覆盖以下内容的 PPTX：

- 标题与正文富文本；
- 多段项目符号；
- 图片与裁剪；
- 矩形、圆形、线条和分组；
- 常规表格；
- 备注与超链接；
- 柱状图、折线图、饼图；
- `appear / fade / fly_in` 中至少一种对象动画；
- 至少一种逐段动画；
- `fade / push / wipe` 中至少一种切换；
- 一个 WPS 中创建的母版或布局；
- 至少一个“识别但不编辑”的对象，如 SmartArt、媒体或公式；
- 一个外部链接关系，用于验证安全扫描和阻断。

测试稿由 WPS 创建并保存，记录初始 SHA-256。

### 5.2 脱敏真实测试稿

选择一份实际使用过的普通演讲或作业 PPT：

- 删除个人、客户与商业敏感内容；
- 保留真实模板、布局密度、字体和图片关系；
- 只存本机，不提交仓库；
- 记录脱敏过程和初始 SHA-256。

## 6. 统一测试操作

每个候选引擎对两份测试稿执行完全相同的操作，输出到独立副本：

1. 读取页面、对象、文本、位置、样式、备注、超链接和动画存在性。
2. 批量替换一个保留局部格式的文本片段。
3. 修改一个标题的位置和字号。
4. 替换一张图片并保持既定裁剪或画框。
5. 新增一个基础形状。
6. 修改一个表格单元格。
7. 更新三类图表之一的数据。
8. 复制并重排一页。
9. 保存为新文件。
10. 用 WPS 重新打开。
11. 检查已有动画、切换、母版、图表和不支持对象是否保留。
12. 用 WPS 导出 PDF并转换为页面图片。
13. 关闭 CLI 自己启动的 WPS 实例并检查残留进程。

候选引擎不支持的操作必须明确记录为 unsupported，不得用图片或重建整份 PPT 静默代替。

## 7. WPS COM 最小能力 PoC

独立编写一次性、最小化验证程序，只验证 COM 可行性，不形成产品架构。

必须完成：

1. 创建独立 WPS 实例。
2. 以不可见或最小化方式打开测试副本。
3. 枚举幻灯片和 shape。
4. 修改一个文本框并保存副本。
5. 添加至少一种对象级动画：优先 `fade`，其次 `appear` 或 `fly_in`。
6. 添加或读取逐段动画。
7. 设置一种页面切换。
8. 创建或更新一种原生图表。
9. 保存、关闭并重新打开验证。
10. 导出 PDF。
11. 正常退出且无交互弹窗、无 CLI 遗留进程。

硬门槛：至少一种简单动画必须能够添加、保存并在 WPS 重新打开后保持有效。若失败，暂停完整开发并重新评估动画实现方式。

## 8. 保真验证

### 8.1 结构验证

保存前后比较：

- slide、shape、关系和媒体数量；
- 文本与 run 样式；
- shape 几何位置；
- 图表部件与嵌入数据；
- 动画 timing 节点与目标对象；
- 页面切换；
- 母版、布局和主题关系；
- 不支持对象的部件和关系；
- 外部链接与宏等风险项。

只允许预期变更。任何无解释的对象消失、关系重建或动画丢失均视为保真失败。

### 8.2 WPS 视觉与播放验证

- WPS 重新打开文件无修复提示。
- 页面无明显换行、字体、图片裁剪和对象位置偏差。
- 已有动画和新增动画可正常播放。
- 页面切换正确。
- 原生图表仍可编辑且数据正确。
- PDF 导出页数与页面顺序一致。

### 8.3 未支持对象验证

- 能识别并报告对象类型。
- 普通编辑时对象原样保留。
- 无法可靠保留时，候选引擎必须在写入前拒绝。

## 9. 原子性与故障测试

对每个候选方案注入以下失败：

- 不存在的对象 ID；
- `expect_count` 不匹配；
- 非法图片路径；
- 无效动画枚举；
- revision 在取得锁后变化；
- WPS 打开超时；
- WPS 保存或导出失败；
- 同一 `request_id` 重复提交。

验收要求：

- 无半成品发布；
- 原件与已确认版不变；
- 错误为结构化 JSON；
- 错误包含稳定代码和 `next_action`；
- 重试不产生重复页面、图片、动画或图表；
- 只终止 CLI 自己启动的 WPS 实例。

## 10. Token 与 Agent 可用性测量

在 PoC 中先做小型代理调用测量，不等完整 MVP：

### 10.1 任务

对一份 10 至 20 页 PPT 执行：

```text
定位第 4 页标题
→ 改文字和字号
→ 替换一张图片
→ 检查变更
→ 渲染第 4 页
```

### 10.2 记录

- 默认 inspect 字节数与估算 Token；
- 获取目标对象所需调用次数；
- patch 长度；
- diff 与 QA 输出长度；
- 是否需要完整文档 dump；
- 是否需要重复渲染未修改页面；
- 总工具调用耗时。

### 10.3 初步目标

- 初次 inspect 默认不超过约 4 KB。
- 单页修改通常控制在 `inspect/diff → apply → qa/render` 三个阶段。
- 无问题页面只返回计数。
- 候选方案的协议可被 `ppt-agent` 压缩到目标格式；若无法压缩且必须暴露大段 XML，则淘汰。

完整 70% Token 目标在六场景 A/B 阶段正式验收。

## 11. 评分矩阵

每项按 0 至 5 分记录，并附证据路径：

| 维度 | 权重 | 硬门槛 |
|---|---:|---|
| WPS 保存与重新打开保真 | 25% | 是 |
| 已有动画与不支持对象保留 | 20% | 是 |
| 原子批量编辑 | 15% | 是 |
| 紧凑 inspect / diff / Schema | 10% | 是 |
| 常规对象编辑覆盖 | 10% | 是 |
| 图表保真和可编辑性 | 5% | 是 |
| 适配代码量与维护成本 | 5% | 否 |
| 离线、安全与供应链可控性 | 5% | 是 |
| 性能与启动成本 | 5% | 否 |

任何硬门槛失败，不能仅靠总分胜出。

## 12. 选择规则

按以下顺序决策：

1. 若某候选引擎通过全部硬门槛，优先选择适配代码量更少、协议更紧凑者。
2. 若 OfficeCLI 直接满足需求，优先薄封装，不重复实现其 OpenXML 能力。
3. 若 hands-on-deck 保真更好且扩展成本可控，基于固定 MIT 版本适配 WPS，并由 WPS COM 补动画。
4. 若两者各有优势但能安全串联，只允许形成一个写入事务，并由 WPS 最后保存和验证。
5. 若两者都不能保留 WPS 文件，停止完整开发，单独评估自研最小 OOXML 写入层的成本。
6. 不因为已经投入时间而降低硬门槛。

## 13. PoC 输出物

完成后应得到：

- `audit/officecli.md`：版本、许可证、安全与行为审计。
- `audit/hands-on-deck.md`：版本、许可证、安全与行为审计。
- `fixtures/synthetic/`：可提交的合成测试材料与生成说明。
- 本机脱敏真实稿及其不入库声明。
- `results/engine-comparison.json`：逐项机器结果。
- `results/engine-comparison.md`：证据摘要与推荐结论。
- `results/wps-com.md`：COM、动画、图表、导出和进程测试。
- `results/token-smoke.md`：初步 Token 与调用次数测量。
- 最终选择记录：直接使用、薄封装、fork 或停止。

临时脚本和中间输出在验证结束后清理；正式保留的 PoC 工具必须进入明确的 `tools/` 或 `tests/` 目录。

## 14. 五阶段路线

### 阶段 1：审计与固定版本

完成两个候选引擎的安全审计、版本固定和许可证记录。

退出条件：候选版本可离线、可重复地运行测试。

### 阶段 2：双材料引擎 PoC

完成统一操作、结构保真、WPS 重开、动画保留、图表和渲染测试。

退出条件：至少一个引擎通过全部硬门槛；否则暂停并重新决策。

### 阶段 3：`ppt-agent` 外壳

实现 JSON 协议、Schema、revision、baseline/candidate、accept/discard、锁、幂等、安全扫描和状态目录。

退出条件：普通编辑事务与故障注入测试通过。

### 阶段 4：WPS COM 能力

补齐动画、切换、三类图表、PDF 导出和最终重开验证。

退出条件：WPS 实机测试通过，且不影响用户已有 WPS 实例。

### 阶段 5：产品闭环

实现模板规范化、QA profile、精简 Skill、独立 EXE和六场景 A/B。

退出条件：达到 MVP 质量门槛和至少 70% Token 降幅。

## 15. PoC 停止条件

出现以下任一情况时停止扩展并重新决策：

- 无法可靠添加并保留至少一种简单动画；
- 普通编辑导致已有动画或不支持对象丢失；
- WPS COM 必须依赖无法自动处理的交互弹窗；
- 无法区分或安全关闭 CLI 自己创建的 WPS 实例；
- 图表只能静默降级为图片；
- 候选引擎必须联网或无法禁用自动更新；
- 紧凑协议无法避免大段 XML 进入 Agent 上下文；
- 两个候选引擎都不能在真实稿上达到基本保真。

停止不是失败，而是防止把不可靠的 PoC 包装成产品。
