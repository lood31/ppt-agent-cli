# MVP 冻结与验收状态

日期：2026-08-12
基线：`v0.1.0-mvp`

## 当前结论

这是供真实试用和 A/B 验证的 MVP 基线，不是稳定发布版本。

- 本地及锁文件隔离环境测试：11/11 通过。
- 当前总覆盖率：52%；`cli.py` 为 0%，`wps.py` 为 15%，属于后续验收重点。
- WPS 动画、原生图表、PDF 渲染和单文件 EXE 已有成功记录。
- 六场景 Token A/B 尚未完成，不能宣称达到 70% Token 降幅。
- 尚未完成 3～5 份真实脱敏 PPT 的一周连续试用，不能宣称可稳定发布。

## 冻结前安全检查

- 未发现凭据、私钥、API Key、客户名或私密材料文件。
- 提交夹具仅包含程序生成的合成 PPTX、合成 PNG 和说明文档。
- `vendor/hands_on_deck` 固定到提交 `a24b996ecff6393ccf39c4fee2b88c493fb0b693`，保留完整 MIT 许可证。
- 三个 vendored Python 文件已从固定提交重新下载并逐文件复核；文本在换行归一化后完全一致，未发现遥测、凭据访问、提权、混淆代码或产品运行时网络请求。
- 冻结前修正了 `NOTICE.md` 中 `replace.py` 的一处哈希抄写错误，并明确记录 Windows checkout 的换行差异。
- 合成 PPTX 的作者和公司元数据为空；PNG 不含附加元数据。
- `dist/`、`.venv/`、覆盖率文件、缓存和私密夹具目录保持 Git 忽略，不进入源码基线。

## 可复现性

- 已生成 `uv.lock`。
- 已在独立临时虚拟环境执行 `uv sync --locked --all-groups`。
- 锁定环境使用 Python 3.12.13，测试 11/11 通过，覆盖率仍为 52%。

## 标签用途

`v0.1.0-mvp` 只表示功能闭环和 PoC 硬门槛已经形成可回退基线。是否进入 Beta，由六场景 A/B 和真实 PPT 连续试用结果决定。
