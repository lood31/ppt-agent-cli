# Beta 前稳定性验证：v0.2.2 最终 B-only

日期：2026-08-13

运行目录：`results/local/token-api/api-gpt54-b-v022-final-r1/`

A 基线：`api-gpt54-final`（封存复用，未重新调用 API）

## 结论

今晚约定的唯一一轮完整 B-only 已执行完毕。六场景产物质量及两项 Token 指标通过，但“错误 CLI 调用为 0”没有通过，因此按既定门槛，本轮整体失败，不进入后两轮稳定性复测，也不能进入 Beta 发布。

- B 总 Token：`1,803,035`；相对固定 A `7,595,417` 降低 `76.26%`，通过。
- B 未缓存输入：`119,666`；缓存输入：`1,654,528`；输出：`28,841`。
- “未缓存输入 + 输出”：A `555,801`，B `148,507`，降低 `73.28%`，通过。
- 工具调用：`124`；API 场景耗时合计 `811.54s`。
- 六个 Agent 进程返回码均为 0，六份 `output.pptx` 均存在。
- 非零工具调用：`9` 次。其中 CLI 参数/调用错误 `7` 次，普通 Shell 文件错误 `1` 次，WPS COM 瞬态失败 `1` 次。
- 单次最大已完成命令文本输出 `12,073 B`，低于 `64 KB`。
- 完整 Schema 读取 `0`；递归搜索、源码搜索、memory 搜索、旧 pptx skill 文档搜索均为 `0`。
- 安全或误改事故：`0`。

## 逐场景计量

| 场景 | A 总 Token | B 总 Token | 总降幅 | A 未缓存+输出 | B 未缓存+输出 | 该项降幅 | B 调用 | 耗时 | 质量 |
|---:|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 1 | 1,693,203 | 411,891 | 75.67% | 153,235 | 46,451 | 69.69% | 27 | 172.76s | 通过 |
| 2 | 1,623,314 | 278,969 | 82.81% | 128,018 | 21,177 | 83.46% | 23 | 127.90s | 通过 |
| 3 | 971,909 | 432,086 | 55.54% | 49,541 | 27,606 | 44.28% | 22 | 184.58s | 通过 |
| 4 | 775,915 | 238,085 | 69.32% | 60,779 | 17,541 | 71.14% | 16 | 121.61s | 通过 |
| 5 | 1,548,092 | 274,954 | 82.24% | 107,836 | 19,850 | 81.59% | 22 | 117.85s | 通过 |
| 6 | 982,984 | 167,050 | 83.01% | 56,392 | 15,882 | 71.84% | 14 | 86.85s | 通过 |

## 质量与结构证据

- `qa --profile presentation`：六场景均为 0 error；场景 1 有 14 条既有引擎 warning，其余场景为 0 warning。
- WPS 12.0 成功回开六份输出，页数依次为 8、4、4、4、4、4，共 28 页。
- 场景 2 的标题、三条正文、按钮、表格和后三页标题均符合任务文本。
- 场景 3 标题为“MVP 真实试用记录”，位置 `x=0.95`；PPTX 内图片哈希与 `replacement.png` 完全一致，图片框保持 `5.8 × 3.2` 英寸。
- 场景 4 标题从 `x=-0.33` 移至 `x=0.0`，字号从 8pt 改为 32pt；已进入画布，内容未变。
- 场景 5 WPS 动画总数恰好为 4：一个 `fade/on_click`，三个由单次逐段语义展开的 `appear/with_previous`，段落范围为 0、1、2。
- 场景 6 第二页标题为“柱状图｜按人工风格续改”，位置 `x=1.05`，相对原位置右移 0.25 英寸。
- 六份输出均由 WPS 成功渲染为 PDF 和逐页 JPG；独立视觉 QA 结果见下节。

## 非零调用审计

| 场景 | 次数 | 分类 | 说明 |
|---:|---:|---|---|
| 1 | 1 | Shell 文件错误 | 在创建 `patch.json` 前读取该文件，`Get-Content` 返回不存在。 |
| 1 | 1 | CLI 路径错误 | 把尚不存在的 `candidate.agent.candidate.pptx` 当作 apply 源文件。 |
| 2 | 1 | CLI 操作错误 | 查询不存在的 `set_table`；结构化返回 `UNKNOWN_OPERATION`。 |
| 3 | 1 | CLI 风险参数遗漏 | 首次 apply 未使用 inspect 已返回的 `--allow-risk external_relationship`。 |
| 3 | 1 | WPS 瞬态失败 | 正确参数的 apply 首次返回 `WPS_COM_FAILED: Open.Close`，同一调用随后重试成功。 |
| 4 | 2 | CLI 字段错误 | 两次把裸 `font_size` 作为 inspect 字段；当前只接受 `style` 或 `shape.font_size`。 |
| 4 | 2 | CLI 参数错误 | 给不支持该参数的 `diff` 和 `inspect` 传入 `--allow-risk`。 |

v0.2.2 已消除上一轮的 here-string 空 stdin、`query`、`slide.number/slide.title` 和裸 argparse 错误，但仍未达到“标准任务第一次调用就成功”。最终输出正确不能抵消过程门槛失败。

## 独立视觉 QA

独立视觉 QA 使用 `view_image(detail=original)` 逐张检查全部 28 页，最终六场景均 PASS：白页、缺图、占位符、裁切、文字溢出、真实重叠、低对比和明显布局破坏均为 0。场景 1 的 14 条引擎 warning 未形成真实视觉缺陷；第 3、5、8 页页脚均完整。场景 5 的动画结构由 WPS COM 独立验证为恰好 4 个效果。

复核过程中，多图预览曾把场景 2–6 的柱状图标签误显示为截断/错位；随后主代理与独立 QA 分别对五个精确绝对路径的 `slide-2.jpg` 单张原图重开，均确认 A/B/C 正确居中于对应柱下，因此撤回该视觉误报。类似地，场景 1 页脚经单张原图重开确认完整。最终结论以单张原图复核为准。

## 决策

按主人“今晚最后测试一次”的限制，本轮后不再调用 API。当前证据只支持以下结论：PPT 产品质量和 Token 目标通过；Agent 调用协议的零误调用稳定性未通过。工作区中的 v0.2.2 改动及本报告保持未提交状态，等待主人后续决定。
