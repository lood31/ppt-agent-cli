# v0.2 Agent 接口减负：B-only API 回归

日期：2026-08-13

运行目录：`results/local/token-api/api-gpt54-b-interface-v02/`

A 基线：`api-gpt54-final`（封存复用，未重新调用 API）

## 结论

接口减负使 Token 指标首次达到 70% 目标。原始六场景运行中的动画场景 5 曾因语义重复失败；定向修复后，场景 5 已通过单次 API、真实 WPS 结构和视觉验收。该定向复测不能单独证明总 Token 稳定达到 70%，因此尚不能宣称 v0.2 可发布。

- B 总 Token：`2,197,951`，低于门槛 `2,278,625`；相对固定 A `7,595,417` 降低 `71.06%`。
- B 未缓存输入：`132,204`；缓存输入：`2,034,944`；输出：`30,803`；reasoning output（输出明细）：`14,650`。
- “未缓存输入 + 输出”：A `555,801`，B `163,007`，降低 `70.67%`。
- 场景 4：A `775,915`，B `323,045`，降低 `58.37%`；不再倒挂并达到至少降低 50% 的门槛。
- 工具调用：A `309`，B `111`，降低 `64.08%`。
- API 耗时：A `2,187.16s`，B `875.04s`，降低 `59.99%`。

## 逐场景计量

| # | A 总 Token | B 总 Token | 降幅 | B 未缓存输入 | B 缓存输入 | B 输出 | A/B 调用 | 质量 |
|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 1 | 1,693,203 | 220,624 | 86.97% | 19,043 | 197,632 | 3,949 | 41 / 22 | 通过 |
| 2 | 1,623,314 | 389,518 | 76.00% | 18,695 | 364,160 | 6,663 | 58 / 14 | 通过 |
| 3 | 971,909 | 543,174 | 44.11% | 31,416 | 504,832 | 6,926 | 51 / 31 | 通过 |
| 4 | 775,915 | 323,045 | 58.37% | 22,190 | 296,576 | 4,279 | 31 / 11 | 通过 |
| 5 | 1,548,092 | 306,761 | 80.18% | 23,034 | 279,680 | 4,047 | 63 / 15 | **失败：动画重复** |
| 6 | 982,984 | 414,829 | 57.80% | 17,826 | 392,064 | 4,939 | 65 / 18 | 通过 |

## 接口与行为审计

- 单项 `schema apply --op move` 输出 `1,426 B`；定向 layout inspect 输出 `1,637 B`；场景 4 QA 修复建议输出 `842 B`。
- 实验内最大单次文本工具输出 `34,711 B`，低于 `64 KB` 门槛；来自完整 `schema apply`，不再是递归仓库搜索。
- 事件日志中没有搜索项目源码、memory 或现有 pptx skill 文档。场景 1 和 5 各有一次非递归 `Get-ChildItem`，仅列当前隔离工作目录。
- 场景 3、4、5 各调用一次完整 `schema apply`；虽未违反 64 KB 门槛，但说明 Agent 尚未完全遵循“只查单项 Schema”的推荐路径。
- 第一次受限运行因网络沙箱无法连接 `api.openai.com`，六场景均为 0 Token、0 工具调用；随后在授权网络环境用同一 run id 续跑，失败行已被成功结果替换，不计入结果。

## 质量证据

- WPS 12.0 成功回开 6/6 输出；页数为 8、4、4、4、4、4。
- WPS 渲染 28/28 页成功。独立视觉 QA 全部通过：无白页、缺图、裁切、异常重叠或明显布局破坏。
- 场景 2 的标题、三条正文、按钮、表格和三个图表标题均符合任务文本。
- 场景 3 标题位于 `x=0.95`；图片位于 `[6.4,1.45]`、尺寸 `[5.8,3.2]`，媒体哈希与 replacement 一致。
- 场景 4 presentation QA 为 0 error / 0 warning，确定性修复正确。
- 场景 6 续改标题为“柱状图｜按人工风格续改”，位置 `[1.05,0.55]`。
- 场景 5 静态视觉通过，但 WPS 结构仍为 10 个效果：1 个对象 fade，加上 9 个重复逐段 appear；预期是 1 个对象效果加 3 个逐段效果。此项明确失败。

## 决策

强类型 Schema、QA `suggested_patch` 和任务定向 inspect 已证明能显著降低 Agent 探索成本，70% Token 指标和场景 4 门槛均达到。动画红灯也已由下述定向复测消除。下一步是六场景各三次独立复测，报告均值、范围与质量通过率；本轮不提前执行。

## 场景 5 定向修复复测

运行目录：`results/local/token-api/api-gpt54-b-interface-v02-s05-fix/`

- 公开 Schema 移除旧 `paragraph`，只接受 `paragraphs: "all"` 或省略；JSON `schema_version` 升级为 `2.0`。
- 同一对象的重复逐段动画、旧/新字段混用、语义相同的重复效果、整体与逐段进入动画冲突，均在 WPS 启动前返回 `DUPLICATE_ANIMATION`；原件、candidate 和 request 状态保持不变。
- 真实 WPS 三段探针：1 条操作、1 次 `AddEffect`，最终恰好展开 3 个效果，OOXML 段落范围为 0、1、2。
- 场景 5 B：input `299,123`（cached `276,608`、uncached `22,515`），output `4,002`，总 Token `303,125`，17 次工具调用，112.18 秒。
- 场景 5 最终结构恰好 4 个效果：对象 fade/on_click 1 个；逐段 appear/with_previous 3 个。WPS 12.0 回开成功，presentation QA 0/0，4 页渲染独立视觉复核通过。
- 无项目源码、memory 或旧 pptx skill 文档搜索；最大单次文本输出 `34,778 B`，低于 64 KB。
- 若仅用本次场景 5 替换原六场景中的失败行，点估计 B 总 Token 为 `2,194,315`，相对 A 降低 `71.11%`。这只是跨两次运行的重构值，不作为三次稳定性证据。
